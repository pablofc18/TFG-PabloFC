CREADA DOCKER DB POSTGRES:

    docker run -d \
    --name postgres_db \
    -e POSTGRES_USER=pablofc18 \
    -e POSTGRES_PASSWORD=pablofc18 \
    -e POSTGRES_DB=mydb \
    -p 5432:5432 \
    postgres:latest


### MOSTRAR codigo python en trabajo:
Puedes optar por incorporar el código en un apéndice del documente final, formateat amb un estil que permeti una lectura fàcil 
(per exemple, amb colors i numeració de línies). Així, al cos principal del treball pots incloure fragments representatius o resums
del codi i, al mateix temps, proporcionar en el apèndix el codi complet (aproximadament 400 línies). També és convenient afegir una 
referència a un repositori en línia (per exemple, GitHub) on es pugui accedir a la versió actualitzada, garantint així que el lector
tingui accés a tota la informació necessària sense saturar el document principal.
###

user1user1 - user9user9 ENTRAID PWD 1P2r3u4e5b6a!