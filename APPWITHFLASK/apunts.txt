CREADA DOCKER DB POSTGRES:

    docker run -d \
    --name postgres_db \
    -e POSTGRES_USER=pablofc18 \
    -e POSTGRES_PASSWORD=pablofc18 \
    -e POSTGRES_DB=mydb \
    -p 5432:5432 \
    postgres:latest

